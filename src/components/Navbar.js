import { padding } from '@mui/system'
import React from 'react'

function Navbar() {
  return (
    <div>
        <h1 style={{textAlign:'center', marginBottom:'25px'}}>React Form Validation with formik and yup</h1>
    </div>
  )
}

export default Navbar